services:
  - type: web
    name: fullstack-app
    runtime: node
    buildCommand: |
      npm config set registry https://registry.npmmirror.com
      npm ci --include=dev
      npm run db:push
      npm run build
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_dNEhL27PWKsg@ep-green-voice-a8mto3l2-pooler.eastus2.azure.neon.tech/neondb?sslmode=require
