services:
  - type: web
    name: fullstack-app
    runtime: node
    buildCommand: |
      npm config set registry https://registry.npmmirror.com
      npm ci --include=dev
      npm run db:push
      npm run build
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:Megaepik@12@db.thgkagjvryghveosxoom.supabase.co:5432/postgres
